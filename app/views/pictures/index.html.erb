<h1>Pictures (<%= @picture.length %>)</h1>
<%= link_to "New picture", "/pictures/new" %>

<% @picture.each do |picture| %>
  <p>Title: <%= picture[:title] %>, Artist: <%= picture[:artist] %></p>
  <%= link_to image_tag(picture.url), "/picture/#{picture.id}" %>
<% end %>